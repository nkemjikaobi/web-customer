/**
 * Routes to access the content management service
 */

import { GATEWAY_API_ROUTE } from "./Authentication";

export const GET_PAGE_CONTENT = `${GATEWAY_API_ROUTE}/api-gateway/get-page-content`;
